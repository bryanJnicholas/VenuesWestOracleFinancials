# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: AzureResourceManagerTemplateDeployment@3
  inputs:
    deploymentScope: 'Resource Group'
    azureResourceManagerConnection: 'FinancialsGLUploadServiceAzureRmPipeline'
    subscriptionId: 'cbd62ea6-dd40-4f50-9260-082f4ceced1e'
    action: 'Create Or Update Resource Group'
    resourceGroupName: 'VW-DEV-ARG-FinancialGLUpload'
    location: 'Australia East'
    templateLocation: 'Linked artifact'
    csmFile: 'VenuesWestOracleFinancials\azuredeploy.json'
    overrideParameters: '-projectName [EnterAProjectName] -linkedTemplateUri [EnterTheLinkedTemplateURL]'
    deploymentMode: 'Incremental'
    deploymentName: 'DeployPipelineTemplate'
